name: eval

on:
  workflow_dispatch:
    inputs:
      pr_number:
        description: 'PR number to evaluate (supports fork PRs)'
        required: true
        type: string
      compressed_dir:
        description: 'Link to compressed_dir zip (e.g. https://github.com/user-attachments/files/26130120/submission.zip)'
        required: true
        type: string

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
          ref: refs/pull/${{ inputs.pr_number }}/merge

      - name: Download and extract compressed_dir
        run: |
          curl -L -H "Authorization: token ${{ github.token }}" -o submission.zip "${{ inputs.compressed_dir }}"
          unzip submission.zip -d .

      - name: Install uv
        uses: astral-sh/setup-uv@v4
        with:
          enable-cache: true

      - name: Install dependencies (cpu)
        run: uv sync --group cpu

      - name: Install ffmpeg
        run: sudo apt-get update && sudo apt-get install -y ffmpeg

      - name: Evaluate
        run: uv run --group cpu python evaluate.py --device cpu --dataloader examples/baseline_dataloader_fast.py

      - name: Print report
        run: cat report.txt
